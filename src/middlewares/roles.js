// Admin  => level 1
// Editor => level 2
// User   => level 3
// a editor can make all of a user and an admin can make all of a user and editor``

const adminRequired = (res, req, next)  => {
    
}

const editorRequired = (res, req, next) => {

}

const userRequired = (res, req, next)   => {

}
